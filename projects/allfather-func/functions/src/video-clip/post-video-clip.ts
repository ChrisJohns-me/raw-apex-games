import * as functions from "firebase-functions";

export const postVideoClip = async (request: functions.https.Request, response: functions.Response<any>): Promise<void> => {
    // TODO
    // if (reportedPlayerId) {
    //     response.status(reportedPlayerId ? 200 : 500).send({
    //         reportedPlayerId,
    //     });
    // } else {
    //     response.status(500).send({
    //         error: {
    //             code: 500,
    //             msg: "Failed to create reported player",
    //         },
    //     });
    // }
};
